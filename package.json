{
  "name": "idh-task",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "browser-sync start --server --files '**/*.css, **/*.html, **/*.js, !node_modules/**/*' --directory --port 7777",
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch style.scss:style.css"
  },
  "author": "Krzysztof Hamerszmit",
  "license": "ISC",
  "dependencies": {
    "browser-sync": "^2.24.5"
  }
}
